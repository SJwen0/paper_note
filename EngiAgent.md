# EngiAgent

核心理解：让大模型不只是“会写看起来合理的工程模型/代码”，而是真正输出满足工程约束、能被求解器验证的可行解。

## 1.简要概括

是一个多智能体系统。把工程问题拆成“分析—建模—验证—求解—评价”五个专家角色，再用一个 **全连接协调器** 动态调度这些角色反复修错，目标是提高开放式工程问题的 **可行解率**，而不是只追求文字表达好看或代码能运行。

## 2.解决了什么问题？

核心问题：LLM 会“说”和“写代码”，但工程问题要求“可行”。论文指出，数学题或代码题往往只要推理正确、程序能跑就算不错；但工程问题更严格，因为结果必须满足 **物理规律、容量限制、安全约束、运行规则、数据一致性** 等现实条件。比如电池容量只有 40 kWh，你不能让模型输出“电池放电 60 kWh”；即使代码能运行，这个结果也是工程上不可实现的。简而言之就是让大模型在工程问题上输出要合理。

作者认为现有 LLM 方法主要有三类问题：

| 现有方向              | 常见问题                                                     |
| --------------------- | ------------------------------------------------------------ |
| 开放式科研/探索 Agent | 喜欢生成新颖想法，但可能停留在概念层面，不能产出可执行、逻辑一致的解 |
| 数学建模 Agent        | 重视模型形式，但不一定输出可行数值解                         |
| 代码生成 Agent        | 代码可能能跑，但可能篡改数据、忽略约束、违反物理规律         |

论文给出的典型错误有四类：**花哨但模糊的建模、修改关键数据、违反物理规律、过度约束模型**。这四类错误共同说明：工程求解的关键不是“答案看起来对”，而是“答案能不能在真实约束下成立”。

### 3.核心创新点

### 创新点 1：把“可行性”放到中心位置

这篇论文不是单纯追求推理分数、文本质量或代码可执行率，而是把 **Feasibility** 作为最重要指标。可行性在论文中指：**最终方案必须与题目数据一致，并满足所有显式或隐式的关键工程约束**。评价时不是让 LLM 主观打分，而是结合可执行数值验证、约束检查和人工专家确认。

### 创新点 2：五个专业 Agent 分工协作

EngiAgent 把工程求解拆成五个角色：

| Agent     | 作用                                                 | 类比           |
| --------- | ---------------------------------------------------- | -------------- |
| Analyzer  | 从题目中抽取变量、参数、约束、目标、隐含工程规则     | 工程需求分析师 |
| Modeler   | 把结构化分析转成可执行模型/代码                      | 建模工程师     |
| Verifier  | 检查模型是否忠实于原问题，是否漏约束、错目标、错数据 | 审核专家       |
| Solver    | 调用求解器运行模型，返回解或错误                     | 数值计算工程师 |
| Evaluator | 综合评价最终方案是否可行、是否高质量                 | 项目验收专家   |

![image-20260515203649565](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260515203649565.png)

### 创新点 3：全连接协调器，而不是固定流水线

传统 Agent 系统常是固定流程：A → B → C → D → E，最后不行就回到某一步。这种方式很僵硬，因为错误可能来自任何阶段，比如数据抽取错了、约束建错了、代码索引错了、求解器失败了。

EngiAgent 的 **（Fully Connected Coordinator）全连接协调器** 允许任意阶段把错误反馈给最合适的 Agent。例如，求解器发现不可行，不一定回到 Modeler，也可能回到 Analyzer 重新理解约束；Verifier 发现语义不一致，也可以直接让 Modeler 重写局部约束。协调器还配有 Memory，记录当前状态、调试历史、成功/失败记录和各 Agent 成功率，用来避免无限循环。

### 创新点 4：验证器不是“代码检查器”，而是“工程可行性守门员”

Verifier 重点检查三类一致性：**语义一致性、约束完整性、数据一致性**。它尤其关注不可妥协的错误，比如目标方向反了、漏掉供需平衡/能量守恒等核心物理规律、关键数据不一致等。它也会区分“本质错误”和“等价表达差异”，避免因为格式不同而反复无效调试。

## 4.论文的完整流程

### 第一步：Analyzer 做问题分析

Analyzer 会把自然语言题目变成结构化建模蓝图。它要识别：

- 决策变量：模型需要决定什么；
- 参数：题目给出的固定数据；
- 约束：不能违反的规则；
- 目标函数：要最小化成本、最大化收益，还是平衡多个目标；
- 隐含工程规则：题目没明说但工程上必须满足的规律，比如能量守恒、容量上限、安全边界。

论文附录中的 Analyzer prompt 要求输出层次化 JSON，包括 `modeling_context`、`core_model_elements`、`extended_analysis_and_robustness` 等内容，目的是让后续代码生成更稳定。

### 第二步：Modeler 生成模型代码

Modeler 根据 Analyzer 的结构化蓝图生成可执行代码。论文主要使用 Pyomo 进行优化建模，但附录也说明 EngiAgent 不局限于 Pyomo。

Modeler prompt 强调：不能写占位符，必须给完整参数；必须定义完整变量、目标函数、约束、求解器调用和结果打印；还要避免 Pyomo 常见错误，比如把 Pyomo 变量放进 Python 的 `if` 判断、`max()`、`min()`、`abs()` 里。

### 第三步：Verifier 检查模型是否真的对应原问题

Verifier 不只是查代码语法，而是检查模型是否“忠实解决原工程问题”。例如：

- 题目要求最小化成本，代码却最大化收益，这是致命错误；
- 电力问题漏掉供需平衡，是致命错误；
- 储能问题漏掉 SOC 动态，是致命错误；
- 数据被偷偷改了，也是致命错误。

Verifier 会给出错误原因和修改建议，供 Coordinator 决定下一步派给谁处理。

### 第四步：Solver 运行模型

Solver 负责真正执行代码、调用求解器、控制时间和资源，避免死循环或长时间卡住。它会区分两种情况：一种是模型成功求出有效结果；另一种是代码错误、求解失败、模型不可行等，并把错误反馈给协调器。

### 第五步：Evaluator 做最终验收

Evaluator 会综合看四个方面：

- 结果是否可行；
- 模型是否对齐原问题；
- 工程逻辑是否合理；
- 整体方案质量是否足够好。

若不合格，Evaluator 会给出改进理由，Coordinator 再决定回到 Analyzer、Modeler、Verifier 或 Solver。

## 5.“全连接协调器”到底怎么工作？

它的本质是一个 **动态错误路由器**。

普通固定流水线像这样：

```
Analyzer → Modeler → Verifier → Solver → Evaluator
```

一旦出错，通常只能按预设路径回退。

EngiAgent 的全连接协调器像这样：

```
任意 Agent 出错
   ↓
Coordinator 判断错误类型
   ↓
查看 Memory 中的历史记录
   ↓
决定派给最可能修好问题的 Agent
   ↓
该 Agent 修改后重新进入流程
```

论文附录给了几个路由例子：比如 Pyomo 导入错误会交给 Modeler；SOC 约束导致非线性结构错误，可能交给 Analyzer 重新设计线性可行表达；节点功率平衡不可行，则先交给 Verifier 查语义映射，再让 Modeler 重写约束。

可以这样理解：
 **固定流水线像一条单行道；全连接协调器像一个项目经理，知道哪个问题该找哪个专家返工。**

## 6. 实验怎么做？结果说明了什么？

### 数据集

作者构建了一个面向工程可行性验证的数据集，共 **53 个开放式工程问题**，来自四类领域：

1. 市场与多智能体决策；
2. 调度与资源分配；
3. 规划与设计；
4. 控制与自主系统建模。

这些任务来自高质量论文，被改写成自包含工程建模任务，并由 20 多位领域专家审阅和完善。

### 评价指标

论文不仅看答案质量，还看是否真的可行。主要评价维度包括：

| 缩写  | 含义                            | 解释                               |
| ----- | ------------------------------- | ---------------------------------- |
| IE    | Information Extraction          | 是否正确抽取数据、变量、约束、目标 |
| DR    | Domain-specific Reasoning       | 是否能用领域知识推理               |
| MO    | Multi-objective Decision-making | 是否能处理多目标权衡               |
| UH    | Uncertainty Handling            | 是否能处理不确定性或信息缺失       |
| Feas. | Feasibility                     | 是否满足工程约束并与数据一致       |

其中 **Feasibility 是最关键指标**

### 实验结果

EngiAgent 在三个大模型上都显著提高了可行解率：

| 模型             | 最强基线可行率 | EngiAgent 可行率 |
| ---------------- | -------------- | ---------------- |
| GPT-4o           | 7.55%          | 64.15%           |
| Gemini-2.5 Flash | 3.77%          | 50.94%           |
| DeepSeek-V3-671B | 28.30%         | 75.47%           |

这说明已有方法可能能生成文本、模型或代码，但很难稳定产出满足真实工程约束的数值解。

还有一个重要现象：DS-Agent 这类方法数值输出率可能高，但可行率很低。例如 GPT-4o 上 DS-Agent 数值解率为 62.26%，但可行率只有 5.66%。这说明“有数值答案”不等于“工程上可用”。

### 消融实验说明了什么？

论文去掉不同模块进行对比，发现：

- 去掉 prompt engineering，信息抽取和建模指导变差；
- 去掉 Verifier，容易漏约束、数据不一致；
- 把全连接协调器换成固定流程，可行率明显下降；
- 去掉 forced switching，质量影响不大，但会增加无效重试。

因此，论文结论是：**Prompt 和 Verifier 是建模/可行性基础；全连接协调器和强制切换机制增强了动态调试能力。**

